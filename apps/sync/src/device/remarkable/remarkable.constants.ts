export const REMARKABLE_CLIENT_FACTORY_TOKEN = Symbol(
  "REMARKABLE_CLIENT_FACTORY_TOKEN",
);

export enum ENTRY_TYPE {
  FOLDER = "CollectionType",
  DOC = "DocumentType",
  TRASH = "trash",
}

export const ROOT_STATE_SYNC_RETRIES = 2;
