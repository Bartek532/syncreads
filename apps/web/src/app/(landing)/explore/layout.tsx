import { ExploreLayout } from "@/components/landing/explore/layout/layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ExploreLayout>{children}</ExploreLayout>;
}
