{
  "name": "rssmarkable",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^18.0.0"
  },
  "scripts": {},
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "prettier": "^2.8.1",
    "husky": "^8.0.0",
    "lint-staged": "^13.1.0",
    "typescript": "^5.2.2"
  },
  "lint-staged": {
    "src/**/*.{js,ts,jsx,tsx}": [
      "eslint --fix",
      "prettier --write \"**/*.{ts,tsx,md}\""
    ],
    "*.json": [
      "prettier --write"
    ]
  }
}
